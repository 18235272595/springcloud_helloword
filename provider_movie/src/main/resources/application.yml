server:
  port: 8000
spring:
  application:
    name: providerMovie
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true   #注册服务时使用IP地址